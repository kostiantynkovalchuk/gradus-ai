AUDIT-ONLY TASK — DO NOT MODIFY ANY FILES. Report findings as a written summary.

Context: Maya HR Telegram bot broadcast feature is misbehaving in group
chat -5226315336. Two symptoms from production logs:

  (A) Only Konstantin (Telegram user 441389791, group owner + developer)
      triggers the "ready to broadcast" confirmation card. Other admins
      in the same group send identical messages/photos but no broadcast
      record is created and no confirmation card is sent.
  (B) When Konstantin clicks the ✅ Confirm button on the card
      (callback_data = broadcast_confirm_<id>), the only side-effect in
      logs is a single answerCallbackQuery. There is NO editMessageText
      on the card, NO sendMessage/sendPhoto fan-out to recipients, and
      NO "[Broadcast] Sent" log line. The same callback also fires
      twice in a row (no idempotency).

Please investigate and report on the following — do not write code, do
not push, do not edit files:

1. Locate services/broadcast_service.py and routes/telegram_webhook.py
   (or whatever the actual paths are). List every function involved in
   the broadcast lifecycle: detect → create → confirm → send.

2. In the message handler that creates broadcasts (the one emitting
   "[Broadcast] Created log id=..., by=..."), identify the EXACT
   authorization check. Specifically answer:
     - Is there a hardcoded user_id allowlist? Paste the literal IDs.
     - Is there a call to bot.get_chat_administrators(chat_id) or
       bot.get_chat_member(chat_id, user_id)? If yes, how is the result
       used? If no, that's the root cause of symptom (A).
     - Is there a chat_id allowlist? If yes, paste the literal IDs and
       confirm whether -5226315336 is in it.
     - Is the photo handler a separate code path from the text handler?
       If yes, do both have the same gate?

3. Locate the callback handler for callback_data starting with
   "broadcast_confirm_". Trace what it does step by step and answer:
     - Does it call broadcast_service.send_broadcast() (or equivalent)?
     - Does it call bot.edit_message_text() / edit_message_reply_markup
       to remove the buttons after click?
     - Is there an idempotency check (e.g. status != 'pending' guard,
       or DB-level row lock) that would prevent a second click from
       re-firing?
     - Does it iterate over recipients? Where does the recipient list
       come from — hr_employee_phone_cache? A separate broadcast
       audience table? Telegram chat members?
     - Is the actual send wrapped in a try/except that might be
       swallowing errors without logging?

4. Check the broadcasts table schema (likely created via a migration
   between 020 and current). Report:
     - The columns (especially status, created_by, chat_id, sent_at,
       recipient_count, error).
     - The current state of rows id=6 and id=7 — what is `status` on
       each? If id=7 is still 'pending' after a confirm click, that
       confirms symptom (B).

5. In routes/telegram_webhook.py, find the source of the
   "⚠️ Legacy /webhook hit — both bots should use /webhook/maya or
   /webhook/gradus" warning. The warning fires on requests whose path
   in the access log is "/api/telegram/webhook/maya" — which is the
   correct new path, not legacy. Identify the faulty path-matching
   logic and explain why it triggers a false positive.

6. Summarize all findings in this order:
     ROOT CAUSE (A) — why non-Konstantin admins don't trigger the card
     ROOT CAUSE (B) — why confirm doesn't fan out
     BONUS — false-positive legacy-webhook warning
     RECOMMENDED FIXES — bullet list, smallest diff possible, but DO
     NOT implement yet.

Output format: plain text report with file paths and line numbers for
every claim. No code changes in this pass.