IMPLEMENTATION — BATCH B (poll logic — isolated to startAnalysis; does NOT touch the
worker queue, renderEngagementWorkspace, or the 45-finding path). Deploy and test
SEPARATELY from Batch A. solomon_template.html, startAnalysis() poll loop ~1022–1049.

B1 — Issue 4 REORDER (the real bug): currently the label-update block (①) runs at the
TOP of poll(), BEFORE the fetch + terminal check (②). On the completing tick this
paints the "longer than usual" warning, THEN fetches, THEN re-renders — so the warning
flashes for the 300–800ms of the fetch and users reload before the auto-render fires.
- Move the label-update block to AFTER the terminal check, INSIDE the "not done"
  branch. New order each tick:
    1. fetch /api/contracts/engagements/{eid}
    2. if status==='analysis_failed' OR docs.some(d=>d.analyzed_at):
         await renderEngagementWorkspace(eid); return;   // label NEVER touched on done
    3. else (still running): update the ticker label, then setTimeout(poll, POLL_MS)
- Result: on completion the results render automatically with NO warning flash and NO
  manual reload. The warning can only appear while genuinely still running.
- Keep the fetch URL, the terminal condition, the renderEngagementWorkspace call, and
  the return IDENTICAL — only the ORDER changes.

B2 — Issue 1 threshold (line 1024): SLOW_WARN_MS = 5*60*1000 → 9*60*1000. With a 5-min
expectation set, "longer than usual" should only fire past ~9 min (above p95), not at
5.

B3 — Issue 1 + 3 ticker text (line 1031, in the "still running" branch after reorder):
  `⏳ Аналізую… (${elapsed}с)`
  →
  `⏳ Аналізую договір… це займає близько 5 хвилин (${elapsed}с)`
  Use lowercase Cyrillic "с" (Issue 3). The "близько 5 хвилин" sets the expectation so
  a climbing counter reads as on-track, not frozen.

After deploy — THE test that proves Issue 4 fixed: start a silpo analysis, then DO
NOT TOUCH the page. Confirm: ticker shows "Аналізую договір… близько 5 хвилин (Xс)"
climbing; NO "longer than usual" warning appears at ~5 min; when analysis finishes
(~5 min) the 45 findings render AUTOMATICALLY with no manual reload and no warning
flash. Confirm still 45 findings (analysis path untouched).