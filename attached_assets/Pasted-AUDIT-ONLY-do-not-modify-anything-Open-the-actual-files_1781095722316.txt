AUDIT ONLY — do not modify anything. Open the actual files and report.

Context: after removing the finding cap, a single-call scan on the Silpo contract
returned 27 findings — but ALL are main-body (§2–§11). Zero from any appendix
(Додаток), including Додаток 8, which the lawyer flagged. The doc is ~107K chars
sent in one scan call. We're considering section-aware scanning (scan main body
and each appendix separately, merge). Before designing it, confirm the following:

1. REDLINE ARCHITECTURE (decides whether a bigger rebuild is needed):
   In analyzer.py, is the counter-wording proposal (the ~400-word "AI пропозиція"
   redline) generated in the SAME LLM call as scan_document, or in a SEPARATE
   call/function per finding? Show the code path. If separate, what triggers it
   and how is it tied back to a finding?

2. HOW THE DOC IS ASSEMBLED FOR THE SCAN:
   Show how raw_text is built before the scan call. Is the whole document
   (body + all appendices) concatenated into one string? Where do the appendices
   sit in that string — and is anything (the 120000-char cap, ordering) cutting
   the tail where the appendices live? Report the char offset where "Додаток"
   sections begin vs the total length.

3. SECTION BOUNDARIES:
   parse_clauses already detects Додаток headers. Can the parsed structure tell us
   where the main body ends and each appendix begins (offsets or clause groups)?
   Report what's available — this is what a section-aware scan would split on.

4. MERGE/DEDUP:
   If we run multiple scan calls and combine findings, where would they be merged,
   and does anything downstream assume a single scan result (the §10.1 guardrail,
   findings INSERT, the analyzed_at completion signal, eval.py)? List every place
   that would need to handle findings arriving from N calls instead of 1.

5. ASYNC COMPLETION:
   The scan runs fire-and-forget via asyncio.create_task with frontend polling
   analyzed_at. If we issue N section calls, how is "done" signaled — only after
   all N complete? Report the current completion mechanism.

Report findings. Do not write the section-aware scan yet — that's the next prompt.