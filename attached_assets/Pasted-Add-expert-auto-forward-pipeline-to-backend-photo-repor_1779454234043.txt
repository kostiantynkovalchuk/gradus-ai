Add expert auto-forward pipeline to backend/photo_report/bot.py.

## AUDIT FIRST

1. Find the function that sends the final report to the agent.
   What is it called? Paste the last 20 lines where 
   sendMessage/reply_text sends the score/errors report.

2. Find where report_id is available after saving to DB.
   Confirm the variable name.

3. Find EXPERT_TG_IDS constant — paste it.

4. Confirm BOT_TOKEN variable name used to send messages.

## IMPLEMENT

After audit, add this forward pipeline:

### Constants (add near EXPERT_TG_IDS):
```python
FORWARD_EXPERT_ID = 408894540  # Александр — receives all new reports
```

### New function (add after existing helpers):
```python
async def forward_report_to_expert(
    context: ContextTypes.DEFAULT_TYPE,
    report_id: int,
    agent_name: str,
    trade_point_name: str,
    report_text: str,
    photo_file_ids: list[str],
    score: int,
    passed: bool
):
    """Forward completed report to expert for review."""
    try:
        passed_emoji = "✅" if passed else "❌"
        header = (
            f"📋 *Новий звіт #{report_id}*\n"
            f"👤 Агент: {agent_name}\n"
            f"🏪 Точка: {trade_point_name}\n"
            f"{passed_emoji} Оцінка: {score}/100\n\n"
            f"Залиш корекцію якщо є розбіжності ↓"
        )
        
        # Send header + report text
        await context.bot.send_message(
            chat_id=FORWARD_EXPERT_ID,
            text=header + "\n\n" + report_text,
            parse_mode="Markdown"
        )
        
        # Forward photos if available
        for file_id in photo_file_ids[:5]:  # max 5
            try:
                await context.bot.send_photo(
                    chat_id=FORWARD_EXPERT_ID,
                    photo=file_id
                )
            except Exception as e:
                logger.warning(f"Could not forward photo: {e}")
                
    except Exception as e:
        logger.error(f"[ExpertForward] Failed to forward report #{report_id}: {e}")
        # Never crash agent flow due to expert forward failure
```

### Wire into report delivery:
Find where the final report text is sent to the agent
(the message with score, errors, shelf share percentages).

After that send_message call, add:

```python
# Forward to expert for review (non-blocking)
# Only forward real reports, skip rejections
if scoring_version == 'v2':
    # Collect photo file_ids that were sent during this session
    # They are stored in context.user_data or passed through
    # the conversation handler — check how photos are stored
    asyncio.create_task(
        forward_report_to_expert(
            context=context,
            report_id=report_id,
            agent_name=update.effective_user.full_name or "Агент",
            trade_point_name=trade_point_name,
            report_text=report_text,  # same text sent to agent
            photo_file_ids=photo_file_ids,  # from context
            score=score,
            passed=passed
        )
    )
```

### Guard — do NOT forward if agent IS the expert:
```python
if update.effective_user.id in EXPERT_TG_IDS:
    return  # expert testing — don't forward to themselves
```

Add this check BEFORE the asyncio.create_task call.

## IMPORTANT NOTES

- The forward must NEVER crash or delay the agent's report
  — use asyncio.create_task + try/except in the function
- photo_file_ids: Telegram file_ids are available when 
  photos are received via update.message.photo[-1].file_id
  Check how bot.py stores received photos during the 
  conversation and use the same stored list
- If file_ids are not easily available, skip photo forward
  and just send the text report — that's enough for corrections
- report_text should be the exact same string sent to agent
  so Александр can reply to it with corrections

## DO NOT TOUCH
- scoring.py
- vision.py
- alex_admin.py
- Any other files
- Correction parsing logic
- Agent-facing messagesSonnet 4.6