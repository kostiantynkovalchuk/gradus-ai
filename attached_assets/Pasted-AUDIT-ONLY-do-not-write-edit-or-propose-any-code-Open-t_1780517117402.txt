AUDIT ONLY — do not write, edit, or propose any code. Open the listed files,
grep for the listed symbols, and report findings. We are adding a pre-auth
Candidate/Employee fork to the Maya HR bot. This audit establishes what is
actually in the code before we design it. Report as a structured list. Do not
design the fix.

1. WEBHOOK ENTRY & ROUTING
- Locate the Maya HR bot webhook handler (the route receiving Telegram updates
  for TELEGRAM_MAYA_BOT_TOKEN). Report file + function name.
- Trace, in order, what happens to an inbound update: where /start is handled,
  where auth (phone whitelist / SED) is triggered, and where message text enters
  HR keyword detection -> preset -> RAG. Report the EARLIEST point we could
  intercept an update BEFORE auth runs.
- Does /start parse a deep-link start payload (e.g. "/start insta")? Show how the
  payload is read, or confirm it is ignored.

2. PER-CHAT STATE
- Is there any per-chat conversational state or "expected next input" mechanism
  (FSM, a state column, in-memory dict, redis)? Report where it lives and how it
  is keyed.
- How does the bot identify a returning chat across messages BEFORE auth — by
  chat_id, user_id, something else?

3. maya_users + SCHEMA
- Dump maya_users columns. Is there a chat_id column, and any field that could
  hold a user_type (candidate/employee)?
- Confirm the latest applied migration number from schema_migrations and/or
  db_migrations.py.
- Show one existing CHECK constraint on an enum-like column as a pattern reference.
- Is there any existing inbound-resume / candidate table?

4. MENU TREE & "ABOUT THE COMPANY"
- Where is the inline-keyboard menu tree defined? Report the callback_data naming
  convention.
- Find the existing "About the company" section. Report exactly where its back
  buttons route (callback_data + the handler that receives it), so we know whether
  a candidate would be dumped into the employee menu.

5. SENDING FILES / REACHING VIKTORIA
- Does the current Maya HR webhook handle message.document at all, or text/photo
  only? Confirm whether Maya HR has ANY file-reception code (Photo Report is a
  separate bot — do not assume its code applies here).
- Is there an existing helper to send a document/file via the bot, or only
  send-message?
- Is there any existing code that sends a message to a specific admin / hr_admin
  user? Report how that admin's chat_id is resolved — this is the pattern we will
  reuse to forward resumes.
- How is Viktoria Kutsaya's user record retrievable — chat_id in maya_users, phone
  in hr_employee_phone_cache, role=hr_admin? Confirm which fields exist and are
  populated for her (do not paste sensitive values).

6. IDEMPOTENCY
- Is there any webhook update_id dedup, or any double-tap guard on callback
  handlers anywhere in the bot? Report the pattern if it exists, or confirm none.