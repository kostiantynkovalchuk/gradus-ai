Clean diagnosis. Two bugs in _protocol_clean():

paras[:2] hard cap — discards content silently. Wrong for legal text where conditions enumerate across many paragraphs.
max_chars=800 — arbitrary cap, also silent (no … warning when paragraphs are dropped before char check).

Plus the multi-paragraph rendering issue: table cells use \n\n joined into one <w:p> with soft line breaks. Doesn't break content, but loses paragraph formatting.
Implementation prompt

Remove paras[:2] truncation entirely. Legal alternatives need full text. Lawyer reads the protocol, the conditions list is the point of the protocol.
Raise max_chars to 4000 as a runaway-protection ceiling only. Findings naturally cap ~2000 chars per stored DB observation. 4000 leaves headroom without enabling pathological output. Append … only if hit — this branch should rarely fire.
Render multi-paragraph as separate <w:p> elements. In build_protocol_docx(), replace the single-run approach with iteration:

python   cell = row[2]
   cell.text = ""  # clear default empty paragraph
   paras = [p.strip() for p in cleaned_text.split("\n\n") if p.strip()]
   for i, para in enumerate(paras):
       p = cell.paragraphs[0] if i == 0 else cell.add_paragraph()
       p.add_run(para)
Same pattern for Col 1 (buyer column) — preserves clause_ref as first run in first paragraph, then iterate remaining content.

Audit downstream impact: check that protocol DOCX table column widths still fit content. Current widths: 454 + 3200 + 3200 + 2506 DXA. Multi-paragraph content with more text per cell may cause row-height issues but should render — python-docx tables auto-grow.
Smoke test: regenerate the Winetime protocol. Confirm row 5 (п.5.17) shows the full (а)/(б)/(в) list with paragraph breaks between conditions.
No DB or schema changes needed. Stored text is already correct.

Out of scope: the 500-char clause_text cap in analyzer.py line 214. That's the buyer column — separate concern. Worth a follow-up audit later, but the verbatim excerpt being 500 chars max is by design (≤25 words / paraphrase rule from §7.2).