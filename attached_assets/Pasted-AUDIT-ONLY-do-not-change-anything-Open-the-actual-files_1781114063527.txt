AUDIT ONLY — do not change anything. Open the actual files and report.

Two symptoms from a live Silpo re-run: (A) 127 findings shown — the cross-section
dedup shipped last deploy did NOT reduce the count (the UI shows many findings
sharing the SAME clause_ref AND category, which dedup should have collapsed);
(B) the analysis is so slow the frontend shows "Аналіз тривалий — оновіть
сторінку вручну" and needs 2-3 manual reloads to land a result.

1. IS DEDUP EVEN RUNNING?
   - Open router.py _analyze_one_document. Confirm _dedup_cross_section_findings
     is actually CALLED (not just defined), on which list, and at what line
     relative to _remove_subsumed_findings and generate_alternatives. Paste the
     call site.
   - Is the call inside a try/except that could swallow an exception and pass the
     undeduped list through? Check for silent failure.
   - Open analyzer.py _dedup_cross_section_findings. Paste it. Confirm the key it
     actually groups on. Does _norm_ref get called on clause_ref before grouping?
   - Find the dedup summary log line ("Cross-section dedup: N dropped"). Was it
     emitted in the last run's logs? If logs aren't accessible, say so. If it WAS
     emitted with "0 dropped" or "127 → 127", report that — it tells us the key
     isn't matching.
   - Check analyzed_at of the engagement vs the last deploy time. Is this 127
     result from CURRENT code, or a stale analysis run before the dedup deploy?
     This决定s whether the bug is "dedup broken" or "stale result".

2. WHY IT'S SLOW — generate_alternatives
   - Open generate_alternatives. Confirm: is it one LLM call PER finding, run
     SEQUENTIALLY (for-loop awaiting each), or batched/parallel? Paste the loop.
   - Roughly how long is one alternative call (model, max_tokens, + Pinecone
     retrieve)? With 127 findings sequential, estimate total wall-clock. Confirm
     this is what's blocking analyzed_at from being stamped.
   - Is the analysis a fire-and-forget asyncio task, and does the frontend poll
     analyzed_at with a hard timeout? Where does the "Аналіз тривалий" message
     get triggered — client-side timeout, or a server signal?
   - Could generate_alternatives be parallelized with bounded concurrency
     (asyncio.gather + semaphore) safely given API rate limits? Report feasibility
     and where the limit would live. Do not implement.

3. THE CAP — where it goes
   - Confirm the exact line in router.py where a global post-dedup, pre-
     generate_alternatives cap would sit. List the per-finding fields available
     there (severity, confidence, grounding/alternative status, clause_ref).
   - Confirm the current per-section scan target ("20–30") and that 6 sections ×
     that target is the structural source of 127.

4. DEDUP KEY SANITY
   - Pull 6 concrete examples from the 127 where the SAME clause_ref appears under
     DIFFERENT categories (e.g. 7.10 Податкова vs Відповідальність; 4.13
     Відповідальність vs Якість; 3.7 Якість vs Доставка). For each, quote the two
     short_notes and state: same underlying risk reworded, or genuinely distinct?
     This tells us whether dedup should key on ref ALONE (collapse category
     splits) or keep category in the key.

Report all of the above. Do not write any fix yet — implementation prompt next.