IMPLEMENTATION — FIX 1 of 3 (recall): accept valid RANGE clause_refs in the §10.1
guardrail, so finding 7.17 (the 10% doc-error penalty) stops being rejected. Audit
confirmed: the finding is FOUND but emitted as range "7.3–7.17", and the guardrail
rejects it — partly because it's a range, partly because a scope-guard blocks it
(the §7 clauses are mis-scoped as 'appendix', so the existing substring match never
runs). BOTH must be handled or the fix won't work. Repo rules: open the file first,
re already imported, this changes the analysis OUTPUT (that's intended). Do NOT touch
the queue, poll loop, protocol, or upload.

LOCATION: analyzer.py, the §10.1 guardrail loop, ~lines 356–411 (confirm current
lines — the rebuild may have shifted them).

THE FIX — add range handling BEFORE the existing per-clause loop rejects the finding:
1. After normalising norm_ref (the lowercased, space-stripped, "п."-stripped
   clause_ref), detect if it's a RANGE: it contains a dash — match any of EN DASH
   (U+2013 –), EM DASH (U+2014 —), or hyphen-minus (U+002D -).
   e.g. re.search(r'[–—-]', norm_ref).

2. If it IS a range:
   - Split on the dash into two endpoints: e.g. '7.3–7.17' → ['7.3', '7.17'].
   - Validate BOTH endpoints look like real clause numbers: each matches
     ^\d+(\.\d+)*$ . If either doesn't, FALL THROUGH to the existing reject behaviour
     (don't accept garbage like 'invalid–7.17').
   - Check BOTH endpoints exist in the parsed `clauses` list (compare against each
     clause's normalised ref, same normalisation as norm_ref). BOTH must be found —
     not just one (per audit: '7.3–7.99' where 7.99 doesn't exist must still reject).
   - CRITICAL — scope relaxation: when validating range endpoints, do NOT apply the
     body-vs-appendix scope guard. The audit found §7 clauses are mis-scoped as
     'appendix', which is why the existing substring match fails. A range is a
     model-level construct spanning blocks the extractor may mis-scope, so match
     endpoints against ANY clause regardless of scope. (Only for ranges — leave the
     scope guard intact for normal single-clause refs.)
   - If BOTH endpoints validate and exist → ACCEPT the finding (found=True), do NOT
     reject, do NOT mark clause_ref_unverified. Log it, e.g.
     "[SolCon] §10.1 Range accepted: clause_ref=%r (endpoints validated) doc=%d".

3. If NOT a range → unchanged: existing single-clause logic and scope guard apply
   exactly as today.

CONSTRAINTS:
- Single-clause findings: behaviour must be byte-identical to today. Only range refs
  get the new path.
- Don't loosen anything for non-range refs — the scope guard stays for them.
- Degenerate/zero-clause sections (clause_count_degenerate path): unchanged.

VERIFY after deploy (this is the proof):
(a) Re-run silpo. In the logs, the §7 range finding (7.3–7.17 or similar) is now
    ACCEPTED, not "Rejected finding: clause_ref='7.3–7.17'".
(b) Query solcon_rejected_findings for the NEW run's doc — there should be NO
    clause_ref_not_found row for the §7 range (it was id 429 on the prior run).
(c) The 7.17 / §7-penalty-block risk now APPEARS in the findings list shown to the
    user.
(d) Finding count: may tick to 46 pre-cap, but cap holds at 45 — confirm still ≤45
    displayed, and that 7.17 made it INTO the 45 (it's a high-severity penalty, so it
    ranks above the medium/low tail — should survive the cap easily).
(e) Spot-check: no OTHER finding changed — single-clause findings identical to the
    prior run. Only the §7 range was recovered.

Report the log line for the range finding (accepted vs rejected) and whether 7.17 is
in the displayed 45.