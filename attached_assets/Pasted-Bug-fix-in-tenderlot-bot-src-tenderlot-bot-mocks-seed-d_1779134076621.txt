Bug fix in tenderlot-bot/src/tenderlot_bot/mocks/seed_data.py
The seed script prints a misleading message after seeding:
Test phones available:
  +380000000001  — Тестовий Користувач (supplier)
  +380675755800  — Апенко Дмитро Сергійович (supplier)
This list is hardcoded as a print statement and does NOT reflect the actual phones in the seeded database. After running sed to swap +380675755800 → +34692480784 in the user data, the print message still shows the old number, which is confusing during testing.
Required changes:

Remove the hardcoded "Test phones available:" block from seed_data.py (or from scripts/seed_mock_db.py, wherever it lives).
Replace it with code that queries the actual database after seeding and prints the real test phones dynamically. Example shape:
python# After seeding, query and print whatever's actually in the DB
async with bot_state.session() as session:  # or whichever engine is correct
    result = await session.execute(
        select(TenderlotUser.phone, TenderlotUser.full_name, TenderlotUser.role)
        .order_by(TenderlotUser.id)
        .limit(5)
    )
    rows = result.all()

print("\nTest phones available (live from DB):")
for phone, name, role in rows:
    print(f"  {phone}  — {name} ({role})")

Additionally, replace +380675755800 (Dmytro Apenko's real phone) in the seed data with +34692480784 as the developer's own test phone. Keep +380000000001 as the generic test entry. Use a literal string in the seed data — no hardcoded business logic around Dmytro's real number.
Make the seed script idempotent and re-runnable: if called against an existing DB, it should detect existing data and either skip seeding OR truncate and re-seed (your choice — document which). Currently the message "Mock data seeded (skipped if already populated)" suggests skip-on-exists, but the print block runs regardless — that's the inconsistency.
Run pytest tests/ -v to confirm no tests broke. Run mypy src/ --strict and ruff check src/ to confirm clean.
Verify the fix manually:
bashrm -f tenderlot_mock.db tenderlot_bot.db
python scripts/seed_mock_db.py
The printed phone list should match what's actually in the DB. Specifically +34692480784 should appear in the output.
Commit with message: fix(mocks): query DB for test phone listing instead of hardcoding