URGENT — a real lawyer (Andrey) is actively testing Solomon Contracts RIGHT NOW.
Fix 1 (range guardrail) is deployed but has a CRASH BUG: any contract that produces a
range finding will crash mid-analysis and leave a "0 risks" result. There's already a
visible broken "Сільпо — 0 ризиків" case (eng 25) in his list. Two changes:

═══════════════════════════════════════════════════════════
PART 1 — FIX THE CRASH (Fix 1a) — analyzer.py
═══════════════════════════════════════════════════════════
Diagnosis (confirmed): the range early-accept path appends a raw LLM dict
({**f, "category":..., "severity":..., "clause_ref_unverified":False}) that is
MISSING document_id, engagement_id, detected_by — which worker.py's DB insert reads
as hard keys → KeyError:'document_id' → analysis crashes → doc stuck, 0 findings.

THE FIX — in the range-accepted branch (~line 405), replace the {**f, ...} append
with the SAME complete, sanitized dict the NORMAL accept path builds (~lines 466–482).
Use these exact fields/values:
    document_id, engagement_id            (function-scope vars, same as normal path)
    clause_ref                            (the range, e.g. '7.3–7.17')
    clause_text: _truncate_clause(f.get("clause_text",""))
    category, severity                    (already computed in branch)
    monetary_exposure_uah: _safe_num(f.get("monetary_exposure_uah"))
    short_note: short_note[:2000]
    proposed_alternative: None
    grounding_status: "ungrounded"
    legal_citations: "[]"
    workflow_state: "triage"
    detected_by: "llm_scan"
    confidence: _safe_float(f.get("confidence", 0.7))
    clause_ref_unverified: False          (endpoints were validated → verified)
- The range DETECTION/validation logic (dash split, endpoint regex, both-exist check,
  scope relaxation) is correct — do NOT change it. Only the appended dict changes.
- Single-clause path: untouched, byte-identical.

ALSO (Rule 3 — defense in depth): wrap the per-finding processing in the guardrail
loop in try/except so ONE malformed finding logs-and-skips instead of crashing the
WHOLE doc. A single bad finding must never take down all 45. This is why eng 25 went
to 0 — one range finding killed everything. Make the loop resilient: bad finding →
log + skip that finding → continue with the rest.

═══════════════════════════════════════════════════════════
PART 2 — CLEAN UP THE BROKEN CASE (eng 25)
═══════════════════════════════════════════════════════════
Eng 25 / doc 76 is stuck (status='running', 0 findings) and shows "Сільпо — 0 ризиків"
in Andrey's case list — looks broken to the lawyer. Either:
- PREFERRED: hide it — UPDATE solcon_engagements SET is_hidden=true WHERE id=25;
  (uses the is_hidden flag from migration 063; removes it from his view, reversible,
  data kept).
- Do NOT just reset it to 'pending' and let it re-run — it's the degenerate/bad upload;
  re-running may just reproduce 0 findings. Hiding is cleaner. If you want it gone
  entirely, hide is still safer than delete.
Leave the GOOD cases visible: "Сільпо — 45 ризиків" (the protocol-ready one) and
Andrey's "Рост — 26 ризиків". Only hide the broken 0-risk eng 25.

═══════════════════════════════════════════════════════════
VERIFY after deploy (use the confirmed-good silpo.docx — has word/numbering.xml,
800 numPr refs, produces the 7.3–7.17 range finding):
(a) Upload silpo.docx to a NEW engagement, run analysis → it COMPLETES, reaches
    'done', ~45 findings, NO crash, NO stuck 'running'.
(b) Log shows "§10.1 Range accepted: clause_ref='7.3–7.17'" AND that finding lands in
    solcon_findings (it's in the displayed 45).
(c) Andrey's case list: the broken "Сільпо — 0 ризиків" (eng 25) is GONE from view;
    the 45-risk Сільпо and 26-risk Рост remain.
(d) No KeyError anywhere in the logs.
═══════════════════════════════════════════════════════════