IMPLEMENTATION. New feature: pre-auth Candidate/Employee fork for the Maya HR bot
(student recruitment campaign). This is MAYA HR — ignore any Solomon Contracts,
.local/session_plan.md, or P1–P7 context entirely; if you find yourself reaching for
a Solomon file, you have drifted, stop. Follow the codebase Rules. Build in the unit
order below. Line numbers from earlier audits have SHIFTED (migrations 057/058 + the
menu consolidation landed since) — locate every site by symbol/structure, grep to
confirm, and if a signature differs from what's described, STOP and report (Rule 2).

GOAL: Anyone opening the Maya HR bot link sees, BEFORE auth, two buttons:
[Кандидат] / [Співробітник].
  - Співробітник → existing auth flow, UNCHANGED (share phone → SED → menu).
  - Кандидат → candidate-only menu: [Про компанію] (reuse existing About section) +
    [Надіслати резюме] (attach a PDF/Word file → bot forwards it to Viktoria Kutsaya).
Candidates get NO free-text Q&A — buttons only.

== UNIT 1: MIGRATION (Rule 6, append-only) ==
- First: SELECT max(version) FROM schema_migrations. Report it. Use the NEXT integer
  (should be 059 — confirm, do not assume). Append a new numbered migration; never
  edit an applied one.
- Two tables, with CHECK constraints in the DB (Rule 7):
  hr_candidates:
    telegram_id BIGINT PRIMARY KEY,         -- private chat_id == telegram_id
    source      VARCHAR NOT NULL DEFAULT 'direct',
    created_at  TIMESTAMP NOT NULL DEFAULT now(),
    CHECK (source IN ('insta','uni','direct','unknown'))
  hr_candidate_resumes:
    id             BIGSERIAL PRIMARY KEY,
    telegram_id    BIGINT NOT NULL,
    file_unique_id VARCHAR NOT NULL,         -- dedup key (stable across re-sends)
    file_id        VARCHAR NOT NULL,         -- volatile, used for copyMessage
    file_name      VARCHAR,
    forward_status VARCHAR NOT NULL DEFAULT 'pending',
    forwarded_at   TIMESTAMP NULL,
    created_at     TIMESTAMP NOT NULL DEFAULT now(),
    UNIQUE (telegram_id, file_unique_id),    -- idempotency guard (Rule 4)
    CHECK (forward_status IN ('pending','forwarded','failed'))

== UNIT 2: IN-MEMORY STATE ==
- Module global in routes/telegram_webhook.py, mirroring the existing
  PULSE_AWAITING_TEXT pattern:
    CANDIDATE_AWAITING_RESUME = {}   # keyed by telegram_id (int)
- Set True when "Надіслати резюме" tapped; read at document dispatch; cleared after
  the forward attempt. Restart-loss is acceptable (candidate re-taps). Durable
  identity lives in hr_candidates, not here.

== UNIT 3: CANDIDATE KEYBOARDS (in hr_keyboards.py — the single keyboard module) ==
Add two small builders next to the existing menu functions (do NOT touch
get_inline_menu_for_access_level — candidates are not in the access_level system):
  get_candidate_fork_keyboard():
    [ 👤 Кандидат ]       callback_data "cand_fork:candidate"
    [ 🧑‍💼 Співробітник ]  callback_data "cand_fork:employee"
  get_candidate_root_keyboard():
    [ ℹ️ Про компанію ]    callback_data "cand_about"
    [ 📄 Надіслати резюме ] callback_data "cand_resume:start"
Wrap any rendering that uses these in try/except with a plain-text fallback (Rule 3).

== UNIT 4: /start — DEEP-LINK + FORK ==
- Locate handle_start_command (services/hr_auth.py). The "/start <payload>" arrives
  as a single text string; split on whitespace to read the payload (currently
  ignored).
- If get_user_by_telegram_id() returns an authenticated employee → existing main
  menu, UNCHANGED. Do not create an hr_candidates row for employees.
- Else, by payload:
    "insta" → upsert hr_candidates(telegram_id, source='insta'); render candidate root
    "uni"   → upsert hr_candidates(telegram_id, source='uni');   render candidate root
    none/other → render the FORK keyboard (get_candidate_fork_keyboard).
- Upsert = INSERT ... ON CONFLICT (telegram_id) DO NOTHING (Rule 4 — repeated /start
  must not duplicate or error).

== UNIT 5: CANDIDATE CALLBACKS + BACK-BUTTON GUARD ==
- In the callback dispatch (the prefix router that currently routes hunt_/admin_cmd:/
  avpost:/hr_/survey_/broadcast_), add a `cand_` branch BEFORE the generic hr_ branch:
    cand_fork:employee  → call the EXISTING share-phone keyboard function (the same
                          one the line-~615 unauthenticated path uses). Do NOT create
                          an hr_candidates row.
    cand_fork:candidate → upsert hr_candidates(source='direct'); render candidate root.
    cand_about          → render the EXISTING "Про компанію" / About content, but its
                          back button must return to the candidate root (cand_about's
                          back → candidate root), NOT hr_menu:main.
    cand_resume:start   → set CANDIDATE_AWAITING_RESUME[telegram_id]=True; prompt
                          (Ukrainian): "Прикріпіть резюме файлом — PDF або Word."
- BACK-BUTTON GUARD (the audited cross-branch bug): at the TOP of handle_hr_callback,
  if the sender has an hr_candidates row AND is NOT an authenticated employee, and the
  callback is hr_menu:main (or any non-cand hr_ callback reached via the shared About
  subtree), render the candidate root menu instead of the employee menu. Single
  chokepoint — prevents a candidate landing on the employee tree via a shared back
  button.
- Before reusing the About content, grep its subtree: if About's child buttons expose
  employee-only callbacks, render a candidate-scoped copy of just the About buttons
  instead of wiring into the employee tree.

== UNIT 6: DOCUMENT INTERCEPT (resume upload) ==
- In _process_webhook_update, the existing document branch is:
    if message.get("document") and not text:
        await handle_document_upload(message, db)   # admin-only
        return {"ok": True}
  Inject a candidate intercept IMMEDIATELY BEFORE it, so a candidate's file never
  reaches the admin-only handler:
    if message.get("document") and CANDIDATE_AWAITING_RESUME.get(telegram_id):
        await handle_candidate_resume(message, db)
        return {"ok": True}
- handle_candidate_resume(message, db):
    * Whole body wrapped in try/except → text fallback (Rule 3).
    * Read document.file_size from the message (no download). If > 20 MB → friendly
      Ukrainian "файл завеликий" message, clear the await flag, return. (Do NOT reuse
      the admin 5 MB MAX_DOC_SIZE — this path forwards by file_id, never downloads.)
    * Accept pdf/doc/docx only (check file_name extension and/or mime_type). Otherwise
      nudge "Надішліть, будь ласка, файл PDF або Word." and KEEP the await flag set.
    * Idempotent insert (Rule 4):
        INSERT INTO hr_candidate_resumes
          (telegram_id, file_unique_id, file_id, file_name, forward_status)
          VALUES (..., 'pending')
        ON CONFLICT (telegram_id, file_unique_id) DO NOTHING
        RETURNING id;
      If no row returned → duplicate (Telegram retry / double-send): skip the forward,
      re-send the confirmation, clear flag, return.
    * Forward to Viktoria by file_id via Telegram copyMessage (NOT getFile/download).
      Grep for an existing copyMessage / send-document-by-file_id helper; if none,
      add a thin one alongside the existing send helpers.
      Caption (Ukrainian): "📄 Нове резюме (джерело: <source>) від <full_name або
      ім'я з Telegram>, @<username або id <telegram_id>>".
    * RECIPIENT = single telegram_id from env VIKTORIA_TG_ID (set to 553961642). If
      unset, fallback: look up Viktoria in hr_users by name and use her telegram_id —
      resolve by NAME→telegram_id, NOT by access_level (her value is 'hr_admin', and
      this path must never broadcast to all hr_admins). If still unresolved →
      forward_status='failed', log, alert admins, but still confirm to the student.
    * On copyMessage success → UPDATE forward_status='forwarded', forwarded_at=now().
      On failure → 'failed' + log + admin alert; do NOT crash. The resume row persists
      either way, so ALWAYS send the student: "Дякуємо, ваше резюме надіслано." (a
      forward failure is recoverable from the table, not the student's problem).
    * Clear CANDIDATE_AWAITING_RESUME[telegram_id].

== UNIT 7: TEXT NUDGE FOR CANDIDATES (no free text) ==
- Locate the unauthenticated branch in process_telegram_message (formerly ~line 615,
  just after get_user_by_telegram_id() returns None — find it by structure, line has
  moved). Before showing the share-phone keyboard: if the sender has an hr_candidates
  row, re-render the candidate root menu with a gentle "Скористайтесь, будь ласка,
  кнопками нижче." Do NOT show the phone keyboard and do NOT fall through to RAG.
  (Enforces buttons-only for candidates.)

== UNIT 8: POST-DEPLOY RECONCILIATION (Rule 5) ==
- Report every other route/handler that imports from the touched files
  (telegram_webhook.py, hr_auth.py, hr_keyboards.py) and confirm they still import
  cleanly.
- Confirm all three modules start with zero import errors.
- Report the migration number actually applied.
- Confirm the existing EMPLOYEE flow is unchanged: an authenticated employee /start
  still lands on the normal main menu, and contact-share auth still works.

Ukrainian for all candidate-facing strings. Button labels above are suggestions — flag
if you change them. env to set on Render after deploy: VIKTORIA_TG_ID=553961642.