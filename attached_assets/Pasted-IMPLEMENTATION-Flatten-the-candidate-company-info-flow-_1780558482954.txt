IMPLEMENTATION. Flatten the candidate company-info flow so candidates never enter the
employee hr_ callback space. This fixes the back-button leak (candidate → employee
menu) by removing the shared-subtree round-trip entirely. Per the completed structure
audit: all four About items are LEAVES (3 videos + 1 text block), content is
candidate-safe (onboarding-level), and content can be REFERENCED from the existing
content source, not duplicated. Two files change: hr_keyboards.py and
telegram_webhook.py (handle_candidate_callback). Follow the Rules. This is MAYA HR —
no Solomon.

== STEP 0: confirm the content helper signature (Rule 2 — do NOT assume) ==
- grep and QUOTE the function the employee side uses to fetch About content (the audit
  called it get_direct_content / fetch_and_send_hr_content). Confirm its real
  signature and return shape: for a given key (e.g. 'video_overview'), what does it
  return — content_type, video_url, text/fallback, title? Quote one real call from the
  employee path.
- Confirm the four content keys exactly as the employee side uses them:
  video_overview, video_values, video_history, section_4_structure.
- If the helper's shape differs from what this prompt assumes, STOP and report before
  coding.

== STEP 1: flatten the candidate root keyboard (hr_keyboards.py) ==
- Edit get_candidate_root_keyboard() to return FIVE buttons, flat, all cand_ callbacks:
    [ 🎬 Загальна інформація   → cand_info:overview  ]
    [ 🎬 Цінності компанії     → cand_info:values    ]
    [ 🎬 Історія компанії      → cand_info:history   ]
    [ 📊 Структура компанії    → cand_info:structure ]
    [ 📄 Надіслати резюме      → cand_resume:start   ]
  (Use a cand_info: prefix so it's clearly candidate-space and distinct from
  hr_content:. Labels copied from the employee About for consistency.)

== STEP 2: delete the cand_about gateway, add four leaf handlers
   (handle_candidate_callback, telegram_webhook.py) ==
- REMOVE the cand_about branch entirely (it currently renders the employee About
  keyboard via create_category_keyboard("about") — that's the leak source). There is
  no more "Про компанію" intermediate level.
- Add handling for the four cand_info:* callbacks. Each one:
    * fetches the SAME content the employee key serves, via the helper confirmed in
      Step 0 — map cand_info:overview→'video_overview', values→'video_values',
      history→'video_history', structure→'section_4_structure'. Reference the existing
      content; do NOT hardcode a second copy of the videos/text.
    * sends it (video with text fallback for the three videos; text block for
      structure) using the same send pattern the employee side uses.
    * attaches a candidate nav keyboard with a SINGLE back button:
        [ 🔙 Назад → cand_root ]
      pointing ONLY at cand_ space. NO hr_menu:*, NO hr_content:*, NO hr_menu:about.
- Add a cand_root callback (or reuse the existing candidate-root render) that
  re-renders get_candidate_root_keyboard(). The Back button on every leaf returns here.
- Wrap each render in try/except with a text fallback (Rule 3).

== STEP 3: verify the leak is closed ==
- grep handle_candidate_callback and the candidate keyboards: confirm NO candidate
  path emits any hr_menu:* or hr_content:* callback. Every candidate-reachable button
  is cand_*. Quote the grep result.
- Confirm the employee About flow is UNTOUCHED: create_category_keyboard("about") and
  the hr_content:* handlers still exist and still serve employees. The candidate path
  now serves the same content via its own cand_info:* handlers; the employee path is
  unchanged. Quote both still present.
- The existing back-button guard in handle_hr_callback (the hr_menu:main → candidate
  root redirect) becomes a BACKSTOP, not the primary defense — LEAVE IT IN PLACE (it
  still catches any edge case), but candidates should no longer ever reach it.
- Confirm all three modules import cleanly.

Report each step with quoted evidence. After this, a candidate's entire reachable
surface is cand_* — no employee callback is reachable, so there is no cross-tree Back
to get wrong.