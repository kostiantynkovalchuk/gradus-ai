IMPLEMENTATION — soft dedup of cross-section duplicate findings. Audit complete.
Root cause: section-aware scan produces the same clause as separate findings
(body 11.5 + appendix 11.5), and _remove_subsumed_findings only collapses numeric
RANGES, never same-ref repeats. A working reference already exists in eval.py
lines 258–263 (normalize_ref + highest-confidence-wins). Port that to production
with one correction. Follow repo rules: open eval.py and router.py and read the
actual code before writing; do not change the scan prompt or the cap in this pass.

1. Add a dedup step in _analyze_one_document, AFTER _remove_subsumed_findings and
   BEFORE generate_alternatives (router.py ~379), operating on the in-memory
   merged all_findings list. No schema change.

2. Dedup key = (normalized clause_ref, category) — NOT clause_ref alone. The data
   model legitimately allows different CATEGORIES on the same clause (e.g. 7.10 as
   both "Відповідальність" and "Штрафи"); those must survive. Only collapse
   findings that share BOTH normalized ref AND category.

3. normalize_ref must: strip leading "п."/"П."/spaces ("п.5" → "5"); treat a bare
   section ref ("9") as LESS specific than a sub-clause ("9.3") or range
   ("9.3–9.11") on the same section — when a bare-section finding collides in the
   same category with a more specific one, keep the more specific. Mirror eval.py's
   normalize_ref where it already does this; extend only as needed.

4. Within each (ref, category) group, keep ONE finding: highest confidence, then
   highest severity (critical>high>medium>low), then longest grounded alternative
   (prefer a finding with proposed_alternative over one without). Drop the rest.

5. Log how many were dropped (count only) so the before/after is visible. Do not
   silently discard — emit a single summary log line.

6. Do NOT add a global cap in this pass. Do NOT touch the "20–30" scan target.
   Report which other readers of all_findings you touched and confirm clean import.

After deploy, re-run silpo.docx and report: total findings before dedup, after
dedup, and the distinct count by severity. We decide on a cap only after seeing
that number.