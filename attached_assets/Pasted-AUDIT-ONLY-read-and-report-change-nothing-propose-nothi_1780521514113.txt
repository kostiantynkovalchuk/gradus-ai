AUDIT ONLY — read and report, change nothing, propose nothing. We are preparing to
consolidate two same-named functions into one. Before designing the merge we must
know whether their differences are accidental drift or intentional
context-specific menus. Report as a list. Do not write the fix.

There are two functions named get_inline_menu_for_access_level:
  A) services/hr_auth.py:~817
  B) services/hr_keyboards.py:~529

1. FULL BODIES: quote BOTH functions in full — signature, every branch, every
   button set per access_level, return shape. Put them side by side.

2. EXACT DIFF: enumerate every difference between A and B:
   - Which access_levels does each handle? (Does one omit a level the other has?)
   - For each access_level present in both, do the button rows differ — labels,
     callback_data, count, order?
   - Do the return types/shapes match exactly (same dict structure Telegram expects)?
   - Any difference in default/fallback behaviour for an unknown access_level?

3. CALLERS — who invokes each, and could they ever produce different output for the
   SAME user?
   - grep every call site of each function (note they share a name, so qualify by
     import path). Report file:line and the access_level passed at each.
   - For A (hr_auth copy): which user-facing flow renders this? (/start greeting?
     menu refresh?)
   - For B (hr_keyboards copy): which flow renders this?
   - CRITICAL: is there any flow where the SAME user, at the SAME access_level, could
     hit A in one path and B in another — and thus see different menus? This is the
     bug-vs-intentional test. If both are always reached identically, the difference
     is pure drift (safe to unify to the correct superset). If they're reached from
     genuinely different contexts that NEED different menus, unifying naively would
     break one context.

4. IMPORT TOPOLOGY for the eventual merge (Rule 5 preview): if we keep B
   (hr_keyboards.py) as canonical and delete A (hr_auth.py), list every importer of
   each file and every call site that would need rewiring. hr_auth.py:818 currently
   imports from hr_keyboards — report that relationship precisely.

5. WHICH IS CORRECT: based on the access_level rename already shipped, confirm both
   copies now use 'hr_admin' (not 'admin_hr') post-057. Then state, per access_level,
   which function's button set is the INTENDED one — or flag where you cannot tell
   from the code alone and I need to decide.

Report only. On your findings we decide: (a) is this safe drift-cleanup or a
context-dependent split, (b) which copy/superset is canonical, (c) the rewire plan.
THEN an implementation prompt.