IMPLEMENTATION. Consolidate the two get_inline_menu_for_access_level functions into
one. Per the audit + the admin_cmd handler audit: Function A (hr_auth.py) is
canonical on every axis; Function B (hr_keyboards.py) is broken (dead admin_cmd:panel
button, missing admin_it, in-place mutation). Keep B's LOCATION (hr_keyboards.py) but
replace its BODY with A's logic. Follow the Rules. Grep each site before editing; if
anything differs from the audit, STOP and report (Rule 2).

== STEP 1: replace Function B's body in hr_keyboards.py (~line 529) ==
Make the canonical function = A's logic, located in hr_keyboards.py:
  - copy-before-mutate: base = create_main_menu_keyboard();
    buttons = list(base.get("inline_keyboard", [])); ... ;
    return {"inline_keyboard": buttons}
    (do NOT mutate base in place — that was B's bug)
  - developer  → A's two rows: [Admin admin_cmd:admin, Stats admin_cmd:stats,
                 Logs admin_cmd:logs] + [Add User admin_cmd:adduser,
                 List Users admin_cmd:listusers]
  - hr_admin OR admin_it → A's row: [Admin admin_cmd:admin, Add User admin_cmd:adduser,
                 Stats admin_cmd:stats]
  - employee / anything else → base only, no admin row
  - DROP the dead "⚙️ Адмін-панель" / admin_cmd:panel button entirely — it routes to
    no handler.
  Keep the docstring; update it to reflect the real button sets.

== STEP 2: delete Function A from hr_auth.py (~line 817) ==
- Remove A's definition.
- A's body contained `from services.hr_keyboards import create_main_menu_keyboard`
  inside it — that local import goes with A.
- Add get_inline_menu_for_access_level to hr_auth.py's import from hr_keyboards
  (the module already imports create_main_menu_keyboard from there — extend that
  import line).

== STEP 3: rewire A's 4 call sites in hr_auth.py ==
All now resolve to the imported canonical function:
  - hr_auth.py:104  get_inline_menu_for_access_level(user.access_level)
  - hr_auth.py:287  get_inline_menu_for_access_level(whitelist_entry.access_level)
  - hr_auth.py:372  get_inline_menu_for_access_level("employee")
  - hr_auth.py:422  get_inline_menu_for_access_level("employee")
The two "employee" sites are output-identical pre/post merge. The :104 and :287 sites
(real access_level) NOW get the correct admin rows for hr_admin/developer/admin_it —
this is the intended fix: those users previously got the full menu via /start but the
broken single-button via contact-share; now both paths agree on A's live buttons.
telegram_webhook.py:495 already imports B from hr_keyboards — no change, it now gets
the corrected body automatically.

== STEP 4 (optional, Rule 3 — separable; flag if you skip) ==
In handle_admin_button_callback (~line 1286), the if/elif chain over cmd has no else.
An unknown admin_cmd:* fires the toast then silently returns {"ok": True}. Add an
else that sends a plain-text "невідома команда" fallback so a future bad callback
isn't a silent no-op. This is independent of the menu merge — keep it a clearly
separate edit.

== STEP 5: reconciliation (Rule 5) ==
After deploy report:
- grep get_inline_menu_for_access_level across backend/ — confirm exactly ONE
  definition remains (hr_keyboards.py) and all call sites resolve to it.
- Confirm importers still import cleanly: telegram_webhook.py (imports from both
  hr_keyboards and hr_auth), hr_auth.py (now imports the function from hr_keyboards),
  main.py.
- Confirm no remaining reference to admin_cmd:panel anywhere.
- Backend starts with zero import errors.
- Migration: none — this is code-only.