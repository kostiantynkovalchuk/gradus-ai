AUDIT + TEST — no production changes. The product requirement: a user uploads the
contract EXACTLY as received from the client (mangled MIME filename, .doc extension,
Word auto-numbering) and Solomon must analyze it correctly with ZERO manual renaming
or cleanup. We need to PROVE this works reliably, not "usually". Do this:

1. Take the confirmed-good silpo file (the one with word/numbering.xml + 800 numPr,
   named =?UTF-8?Q?=D0=91=D0=91.doc — the EXACT raw form a client sends). Run the
   ACTUAL ingestion + numbering-aware extraction on it (the real production code path,
   not python-docx plain text). Report:
   - Does extract_text resolve the clause numbers (7.3, 7.17, 5.4, 3.10) into the
     extracted text? Paste a snippet showing resolved clause refs.
   - Does the clause parser then produce NON-degenerate sections (real sub-clause list)?
   - Confirm this is the SAME path docs 74/75 used (the 45-finding runs).

2. Now the key question — WHY did eng 25 (doc 76) go all-degenerate on a file that
   should be identical? Compare doc 76's stored bytes vs doc 74's stored bytes:
   - Are they byte-identical, or actually different files/versions?
   - If different: eng 25 was a DIFFERENT upload (bad version), and the .doc path is
     fine — docs 74/75 prove it. The degenerate run was a one-off bad file, not a
     systemic routing bug.
   - If byte-identical but one worked and one didn't: there's a NON-DETERMINISTIC
     extraction bug (e.g. antiword sometimes installed/firing on Render, sometimes
     not) — that's a real reliability problem we must fix (force the docx path by
     content-sniffing magic bytes, never trust extension).

3. RELIABILITY FIX (report, don't implement yet): to GUARANTEE a client's raw file
   always hits the numbering-aware extractor regardless of extension — should
   ingestion sniff magic bytes (PK zip header = real docx → docx extractor; OLE
   header = real legacy doc) instead of trusting the filename extension? Report what
   that change looks like and whether it removes the antiword risk entirely.

Bottom line we need: PROOF that the raw client file (mangled name, .doc ext, Word
numbering) reliably produces the 45-finding result with no manual steps — and if it
DOESN'T reliably, the exact fix to make ingestion format-detect by content not
extension.