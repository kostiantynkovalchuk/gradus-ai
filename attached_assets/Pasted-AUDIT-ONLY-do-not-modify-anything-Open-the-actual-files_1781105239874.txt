AUDIT ONLY — do not modify anything. Open the actual files and report.

Context: section-aware scanning works (it now reaches the appendices), but it
removed the per-section finding cap AND it surfaces heavy duplicates. The Silpo
run returned 127 findings, of which a large share are the SAME clause found
twice: body 11.5 vs appendix 11.5, 7.15 ×2, 5.17 ×3, 2.10 ×3, plus a cluster of
bare-number findings (2, 3, 4, 8, 9, 11, 14, 16, 17, п.5, п.7) that are appendix
re-detections of body clauses. Stripped of dupes it's ~40–50 distinct risks.
The merge step (_remove_subsumed_findings, run once on the merged list) is not
collapsing these. Report the following before any fix:

1. FINDING KEY / DEDUP LOGIC
   - Open _remove_subsumed_findings. Report exactly what it compares: clause_ref?
     scope? short_note text? severity range? Paste the comparison.
   - Confirm whether it treats body "11.5" (scope=body) and appendix "11.5"
     (scope=appendix:N) as the same finding or different. State why two findings
     on the same clause with near-identical short_note survive as separate rows.
   - Is there ANY dedup keyed on (clause_ref + similar text) regardless of scope?
     If not, say so plainly.

2. WHERE MERGE HAPPENS vs WHERE FINDINGS ARE COUNTED
   - In the router section loop, show the order: scan each section -> accumulate
     all_findings -> _remove_subsumed_findings(all_findings) -> generate_alternatives
     -> INSERT. Confirm dedup runs BEFORE generate_alternatives and BEFORE insert
     (so we don't pay for alternatives on dupes, and don't store dupes).
   - Report how the "27 -> 127" count happened: how many sections ran, and the
     per-section finding count, so we can see whether one section flooded or all
     of them did.

3. THE CAP
   - Confirm the per-call/per-section finding target currently in the scan prompt
     (after we changed it to "20–30"). With ~6 sections that's the source of 127.
   - Report where a GLOBAL cap could be applied post-merge (e.g. keep top N
     distinct by severity) without re-introducing the old recall bug. Do not
     implement — just identify the location and what data is available there
     (severity, confidence, grounding_status).

4. BARE-NUMBER / WEAK REFS
   - The findings with refs like "2", "3", "9", "п.5" — which section scope do
     they come from? Are they appendix findings whose clause numbers didn't
     resolve (flag-don't-drop path), or body findings with stripped refs?
   - Report whether these are distinct risks or mis-anchored duplicates of body
     clauses, with 2–3 concrete examples from the 127.

5. PRODUCT BEHAVIOUR (report current, do not decide)
   - Does the data model allow multiple distinct findings per clause, or is one-
     per-clause assumed anywhere downstream (frontend grouping, DOCX export,
     accept/reject)? This decides whether dedup should be hard (one card per
     clause) or soft (drop true repeats only).

Report findings. Do not write the dedup fix or re-add the cap yet — next prompt.