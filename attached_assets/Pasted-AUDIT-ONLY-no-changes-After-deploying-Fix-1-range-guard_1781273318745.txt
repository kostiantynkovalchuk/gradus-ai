AUDIT ONLY — no changes. After deploying Fix 1 (range guardrail), a live Silpo
re-run on engagement 25 is BROKEN: analysis ran ~532s (~9 min, vs normal ~5), the
worker logs "executed successfully" every 15s but with NO "Claimed doc" / NO
section-scan / NO findings lines, and the frontend is stuck on the 9-min "довше
звичайного" banner. There's also "ERROR:solomon_bot:Solomon search error: Expecting
value: line 1 column 1 (char 0)". Diagnose — do NOT fix yet.

1. Engagement 25 / its doc: query analysis_status and finding count. Is the doc
   stuck in 'running'? 'failed'? Did it ever get claimed? Did findings get written
   (how many)? Did it reach 'done'?
2. Is the worker actually claiming doc for eng 25? The logs show the 15s job
   "executed successfully" repeatedly with NO "Claimed doc" line. Either (a) the doc
   isn't eligible (wrong analysis_status / document_type), or (b) it WAS claimed,
   errored mid-analysis, and got marked 'failed' so subsequent ticks find nothing.
   Which? Check the worker's claim query result and the doc's current status.
3. THE FIX-1 SUSPECT: did the range-guardrail edit cause an exception during the
   guardrail loop? Re-read the exact block inserted in analyzer.py §10.1. Could it
   throw on a normal single-clause ref or a malformed range — e.g. split producing
   !=2 parts, an endpoint regex on None, an index error, the early-accept path
   referencing a variable not yet defined? Wrap-check: is the new block inside the
   try/except that protects the worker, or could it crash the whole analyze run?
4. The JSON error "Expecting value: line 1 column 1": trace its source. Is it in the
   CONTRACTS analysis path (scan/alternative json.loads), or is "solomon_bot" the
   separate Telegram COURT-search bot (unrelated)? Confirm which — it may be a
   red herring or it may be the analysis dying.
5. Compare: was the LAST good run (eng 23/24, pre-Fix-1) clean? What's different in
   the logs between that run and eng 25?

Report the doc's analysis_status + finding count FIRST — that one fact tells us if it
finished broken, died mid-run, or never started. Then the Fix-1 exception check.
Do NOT implement anything.