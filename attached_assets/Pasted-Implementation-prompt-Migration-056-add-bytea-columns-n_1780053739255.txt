Implementation prompt

Migration 056 — add bytea columns, nullable:

sql   ALTER TABLE solcon_documents       ADD COLUMN file_bytes BYTEA;
   ALTER TABLE solcon_protocols       ADD COLUMN docx_bytes BYTEA;
   ALTER TABLE solcon_legal_opinions  ADD COLUMN docx_bytes BYTEA;
Keep existing path columns — they become metadata-only (filename derivation), don't drop. Existing 37 rows get NULL bytes; that's fine, they were already orphaned.

Writes — store bytes alongside path:

ingestion.save_upload() — change signature to return (path, bytes); caller stores both.
_save_document() in router.py — INSERT file_bytes from upload data.
POST protocol (line 671) — INSERT docx_bytes from build_protocol_docx return value.
POST legal-opinion (line 769) — same pattern.


Reads — disk-only paths replaced:

GET protocol (line 702) — SELECT docx_bytes, return Response(content=row["docx_bytes"], media_type=...). Drop the FileResponse and the orphan 404 branch entirely. If docx_bytes IS NULL, return 404 with {"code": "protocol_missing", "message": "Протокол не знайдено"} — Ukrainian message, structured detail.
GET legal-opinion (line 782) — same pattern, same structured 404.


Re-extract + OCR — tempfile pattern:

In re_extract_document: read file_bytes from DB, write to tempfile.NamedTemporaryFile(suffix=...) matching the mime type, pass that path to extract_text(). Delete temp file after extraction returns.
For OCR path: temp file must survive the background thread. Pattern: create temp file with delete=False, pass path to run_background_ocr, the OCR thread deletes the file in a finally block after all pages process.
Suffix matters: pdf2image and pdftotext dispatch on file extension. Derive suffix from mime_type or original filename — .pdf, .docx, .doc.


Upload handler — keep writing to disk for the immediate-OCR case (avoids the round-trip of write-bytes → read-bytes → write-temp), but also store bytes in DB. After successful insert, the disk file is best-effort cleanup — its survival doesn't matter.
Frontend — no changes. The endpoints' contracts are unchanged from the client's perspective.
Post-deploy reconciliation:

Grep for storage_path and docx_storage_path reads — confirm every disk-read path either uses the new bytea column or is dead code.
Confirm no other code holds Path objects expecting them to resolve after restart.


Smoke tests:

Upload a new contract → confirm file_bytes IS NOT NULL in DB.
Trigger re-extract on the same doc → confirm it works (uses bytea path, not disk).
Generate protocol → download → restart backend → download again. File should still come down.
Same for legal opinion.
For a scanned PDF, trigger OCR — confirm temp file is created, OCR completes, temp file is cleaned up.


Out of scope: deleting orphaned solcon_documents rows for the existing 22; the user can re-upload. Migrating off bytea to S3 later if size grows. The docx_storage_path text columns staying around is intentional — useful for filename hints, no harm.