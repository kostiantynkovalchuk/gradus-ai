IMPLEMENTATION — speed + robustness. Full doc-71 run confirmed: 45 findings, ~8 min
total. Alternatives phase is now parallel/cached (good). The SCAN phase (~5 min) is
the bottleneck and is still SERIAL. No 429s in logs — not a rate-limit issue.
Repo rules: open files first, report import reconcile, don't change the 45 output.

FIX 1 — PARALLELIZE THE 5 SECTION SCANS (biggest win)
- Open the section loop in worker.py (the `for section in sections:` that calls
  scan_document per section, serial). The 5 scans are independent (each takes its
  own section text, no shared mutable state between them).
- Wrap them in a ThreadPoolExecutor(max_workers=5) exactly like generate_alternatives
  already does — submit all 5 section scans, collect results. Preserve per-section
  logging and the guardrail step that runs on each section's findings.
- CAUTION: the scan calls use the shared ANTHROPIC_API_KEY. 5 concurrent scan calls
  + (later) parallel alternatives is more simultaneous load. Logs show NO 429s today
  at 5-wide alternatives, so 5-wide scans should be fine — but confirm no shared
  mutable state (the clause-parse results, the findings list) is written concurrently
  without protection. Each section must append to its own result, merged after.
- Expected: scan phase drops from ~5 min to ~the slowest single section (~2.5 min,
  the body).

FIX 2 — TOLERANT JSON PARSE FOR ALTERNATIVES (Rule 3)
- analyzer.py line ~577, _process_one: json.loads(raw_alt) crashed on a response
  that began "Here is my analysis of the retrieved sources:..." — the model
  returned prose, not JSON. The run survived (45 findings) but that finding lost its
  alternative.
- Before json.loads: strip leading/trailing prose — extract the JSON object (find
  first '{' to last '}', or a ```json block) and parse that. If no JSON found, log
  and fall back gracefully (keep the finding, empty/default alternative) — never let
  one chatty response crash a finding. Confirm the except already prevents a hard
  failure; make it extract-then-parse.

FIX 3 — LOCK THE CACHE INIT (minor)
- corpus.py: the _INDEX_CACHE / _OPENAI cache None-check isn't thread-safe — 5
  threads created the client 5× at once ("Pinecone Index client created" ×5). Add a
  threading.Lock around the init so it's created once. Low priority; works after the
  burst regardless.

FIX 4 — FRONTEND STILL NOT DEPLOYED (separate from above)
- The browser STILL shows the old alert("Аналіз тривалий — оновіть сторінку вручну").
  The template edit never reached production. Report: is solomon_template.html
  served from /app/frontend/dist on Render, and does deploying require a frontend
  BUILD step (npm run build) + commit of the dist, separate from the backend deploy?
  Identify exactly why the edit didn't ship and what command/step deploys it. Then
  ensure the alert-removal edit is actually in the deployed dist.

After deploy, re-run silpo, report: (a) scan phase wall-clock (expect ~2.5 min, down
from ~5), (b) total wall-clock, (c) still 45 findings / 10-29-6 / total==distinct,
(d) the JSON parse error is gone or handled gracefully, (e) the browser shows the
calm ticker, NOT the old alert.