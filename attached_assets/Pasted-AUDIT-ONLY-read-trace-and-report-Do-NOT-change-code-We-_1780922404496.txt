AUDIT ONLY — read, trace, and report. Do NOT change code. We are launching the
student campaign with a BARE bot link (https://t.me/MayaGradusBot) — NO ?start
payload — because the Instagram post can't carry a clickable/tracked link. We need to
confirm the FULL candidate user flow works end-to-end via bare /start, since that is
now the path every campaign student takes. This is MAYA HR — no Solomon. Report each
step PASS/FAIL with quoted code or quoted DB result. Where only a live Telegram tap
can confirm something, say so explicitly and mark it "MANUAL TEST NEEDED".

CONTEXT: payload deep-links (?start=insta / ?start=uni) still exist and still work —
we just won't USE insta. The bare /start path is the focus. Confirm nothing in the
bare path depends on a payload being present.

1. BARE /start — quote handle_start_command. Trace what happens when payload is EMPTY
   (no arg after /start):
   - For an UNAUTHENTICATED user (no hr_users row): does it render the
     [Кандидат]/[Співробітник] FORK? Quote the branch. Confirm empty payload does NOT
     error, does NOT skip to a broken state, does NOT require a payload value.
   - For an AUTHENTICATED employee: confirm it still goes straight to the employee
     menu, payload ignored.
   - Confirm there is no code path where empty/missing payload throws (e.g. an
     unguarded split()[1] index, a .get on a missing key).

2. FORK → CANDIDATE — the bare-link student taps [Кандидат] (cand_fork:candidate).
   Quote that branch. Confirm:
   - it upserts an hr_candidates row with source='direct' (since no payload);
   - the upsert is ON CONFLICT DO NOTHING (a returning student who taps again doesn't
     error or duplicate);
   - it renders the candidate root menu (the 6-button, 3-row layout).
   This is the exact path no insta-payload test exercised — confirm source is set
   HERE, at the fork tap, not only at /start.

3. FORK → EMPLOYEE — taps [Співробітник] (cand_fork:employee). Confirm it routes to
   the existing share-phone flow and does NOT create an hr_candidates row. (Regression
   check: employees who arrive via the bare campaign link must still be able to auth
   normally.)

4. CANDIDATE MENU — full surface. For each of the 6 buttons trace the handler:
   - cand_info:overview / values / history / structure → serves the right
     video/text, back button → cand_root only (no hr_ callback).
   - cand_vacancies → queries hr_candidate_vacancies WHERE is_active=true, renders URL
     buttons; empty-state message if none; back → cand_root.
   - cand_resume:start → sets the awaiting-resume state, prompts for file.
   Confirm EVERY back/nav button stays in cand_ space (re-run the hr_ leak grep, quote
   result).

5. RESUME PATH via bare-link candidate — trace handle_candidate_resume for a user
   whose source='direct':
   - the forward to Viktoria (VIKTORIA_TG_ID) does NOT depend on source/payload in any
     way — confirm source is purely a label and does not gate the forward;
   - idempotent insert + copyMessage path unchanged;
   - student gets confirmation regardless.

6. SOURCE COLUMN REALITY — query hr_candidates GROUP BY source. Report current
   distribution. Confirm 'direct' is a valid value the code writes and nothing
   downstream (analytics, dashboard, any query) BREAKS or hides candidates whose
   source='direct'. If any code filters candidates by source IN ('insta','uni') and
   would EXCLUDE 'direct', flag it — that would make bare-link students invisible.

7. ?start=uni STILL WORKS (the uni page keeps its payload) — confirm the uni deep-link
   path still upserts source='uni' and lands on the candidate menu, unaffected by us
   not using insta.

8. MANUAL-TEST CHECKLIST — list the specific things only a live Telegram tap can
   verify (you cannot tap buttons): bare /start → fork renders; tap Кандидат → 6-button
   menu renders correctly on mobile (2-per-row, labels not truncated); tap Вакансії →
   3 live links; send a PDF → reaches Viktoria + confirmation. Produce this as a clean
   checklist for the human to run.

Report only. Flag anything in the bare-/start path that assumes a payload, and
anything that would make source='direct' candidates invisible downstream.