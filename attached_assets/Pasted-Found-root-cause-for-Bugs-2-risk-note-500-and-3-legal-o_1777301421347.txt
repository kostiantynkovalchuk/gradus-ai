Found root cause for Bugs #2 (risk-note 500) and #3 (legal opinion fail). Same JSONB-double-parse bug from this morning, surfacing in two more code paths that the previous fix didn't cover:
artifacts.py:90       cits = json.loads(f.get("legal_citations", "[]"))
analyzer.py:438       "legal_citations": json.loads(f.get("legal_citations", "[]"))
psycopg2 RealDictCursor auto-parses JSONB to a Python list. Both call sites then call json.loads() on the already-parsed list and crash with TypeError: the JSON object must be str, bytes or bytearray, not list.
Fix: apply the same isinstance guard used in _analyze_one_document to both locations. Either inline:
pythonraw_cits = f.get("legal_citations") or []
cits = raw_cits if isinstance(raw_cits, list) else json.loads(raw_cits)
Or extract a small helper since this pattern now appears 4+ times:
pythondef _safe_jsonb(value, default):
    if value is None:
        return default
    if isinstance(value, (list, dict)):
        return value
    return json.loads(value)
Then audit the codebase for any remaining json.loads(f.get("legal_citations"... or json.loads(f.get("clauses"... patterns and apply consistently. Likely candidates: anywhere findings or documents are fetched and serialized to a different format.
After the fix:

Test risk-note download (GET /api/contracts/engagements/1/risk-note.docx)
Test legal opinion generation (POST /api/contracts/engagements/1/legal-opinion)
Both should succeed without re-running analysis (use existing engagement 1)

Then proceed to issues #1 (protocol_draft label), #4 (severity translation), #5 (English AI tag in finding text + SQL backfill) from the previous message — those are independent of this fix.

That's a clean 10-minute fix. Same root cause as this morning, just incomplete coverage. Once it lands, the three remaining issues are pure UI/translation polish.
A meta-observation worth flagging
This is the second time today the same bug class has surfaced in a different file. Both fixed by the same one-line change. The pattern suggests a missed code review opportunity: when Replit fixed _analyze_one_document this morning, the right next step was to grep the entire codebase for json.loads(f.get( and verify all instances. Instead, it was fixed point-by-point as bugs surfaced.
Worth telling Replit explicitly:

When you ship this fix, do a full grep for json.loads( and json.loads(f. patterns across all of backend/solomon_contracts/. Make sure every JSONB-column read goes through the safe-parse helper. Don't wait for a third bug report to find a third instance.