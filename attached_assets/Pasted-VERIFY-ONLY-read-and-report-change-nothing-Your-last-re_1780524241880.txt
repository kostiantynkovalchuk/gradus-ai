VERIFY ONLY — read and report, change nothing. Your last report claimed all 8 fork
units complete but then drifted into Solomon P1–P7 re-verification, which was NOT
part of this task and which the prompt explicitly forbade. Discard all Solomon work
from your report. I am verifying the MAYA HR FORK actually landed as specified. For
each item, QUOTE the on-disk code/result — do not summarize, do not assert "done."

1. MIGRATION 059
   - SELECT version FROM schema_migrations — confirm 059 present, quote the row.
   - \d hr_candidates and \d hr_candidate_resumes — quote actual columns + CHECK
     constraints. Confirm the UNIQUE (telegram_id, file_unique_id) exists.

2. DISPATCH ORDER (the critical correctness point) — quote the relevant region of
   _process_webhook_update. Confirm the candidate-document intercept
   (CANDIDATE_AWAITING_RESUME check) appears textually BEFORE the admin-only
   `handle_document_upload` branch. If it's after, a candidate's PDF hits the
   admin-only handler and is rejected — quote the order to prove which it is.

3. cand_ CALLBACK BRANCH — quote the dispatch. Confirm `cand_` is matched BEFORE the
   generic `hr_` branch (prefix order matters: hr_ would not catch cand_, but confirm
   cand_fork/cand_about/cand_resume each route correctly).

4. BACK-BUTTON GUARD — quote the top of handle_hr_callback. Confirm: a sender with an
   hr_candidates row who is NOT an authenticated employee, hitting hr_menu:main, is
   redirected to candidate root. Confirm the guard checks BOTH conditions (has
   candidate row AND not employee) — an employee must still reach the employee menu.

5. EMPLOYEE FLOW UNTOUCHED (regression check) — confirm an authenticated employee
   /start still renders the normal main menu and contact-share auth still works.
   Quote the employee branch of handle_start_command. This is the "nothing changes
   for employees" guarantee.

6. RESUME FORWARD — quote handle_candidate_resume in full. Confirm:
   - file_size > 20MB rejected without download;
   - pdf/doc/docx only;
   - idempotent INSERT ... ON CONFLICT DO NOTHING RETURNING id, with the no-row-
     returned (duplicate) path skipping the forward;
   - copyMessage (NOT getFile/download) to the recipient;
   - recipient = env VIKTORIA_TG_ID, fallback by NAME→telegram_id (NOT access_level,
     NOT a broadcast to all hr_admin);
   - student always gets the confirmation even when forward_status='failed';
   - whole body in try/except (Rule 3).

7. TEXT NUDGE — quote the unauthenticated branch in process_telegram_message. Confirm
   a sender with an hr_candidates row gets the candidate root menu, NOT the phone
   keyboard and NOT RAG.

8. IMPORTS — confirm all three modules (telegram_webhook.py, hr_auth.py,
   hr_keyboards.py) import with zero errors. Quote the actual import-check output, not
   "OK."

Report each as PASS (with quoted evidence) or FAIL/UNSURE. Do NOT touch Solomon. Do
NOT edit anything. If any unit is missing or wrong, just report it — we fix after I
see the real state.